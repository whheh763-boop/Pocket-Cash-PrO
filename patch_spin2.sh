sed -i 's/if (!isSpinning && spinsLeft > 0) {/if (!isSpinning \&\& spinsLeft > 0) {\n                        if (System.currentTimeMillis() - lastSpinTime < 5000) {\n                            android.widget.Toast.makeText(context, "Please wait 5 seconds between spins.", android.widget.Toast.LENGTH_SHORT).show()\n                            return@Button\n                        }\n                        lastSpinTime = System.currentTimeMillis()\n                        scope.launch { val tg = android.media.ToneGenerator(android.media.AudioManager.STREAM_MUSIC, 100); for(i in 1..10) { tg.startTone(android.media.ToneGenerator.TONE_CDMA_PIP, 50); kotlinx.coroutines.delay(350) }; tg.release() }/g' app/src/main/java/com/example/ui/screens/SpinWheelScreen.kt
