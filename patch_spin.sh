sed -i 's/val rotation = remember { Animatable(0f) }/val rotation = remember { Animatable(0f) }\n    var lastSpinTime by remember { mutableStateOf(0L) }/g' app/src/main/java/com/example/ui/screens/SpinWheelScreen.kt
sed -i 's/if (!isSpinning) {/if (!isSpinning) {\n                        if (System.currentTimeMillis() - lastSpinTime < 5000) {\n                            android.widget.Toast.makeText(context, "Please wait 5 seconds between spins.", android.widget.Toast.LENGTH_SHORT).show()\n                            return@Button\n                        }\n                        lastSpinTime = System.currentTimeMillis()/g' app/src/main/java/com/example/ui/screens/SpinWheelScreen.kt
